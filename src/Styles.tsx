import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  fabStyle: {
    bottom: 16,
    right: 16,
    position: 'absolute',
  },
  listCard: {
    padding: 5,
    marginVertical: 8,
    marginHorizontal: 16,
  },
});

export default styles;
